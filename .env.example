# Public canonical origin used by Metadata, sitemap and mock DTO links.
SITE_URL=https://example.com

# Required for production mock sessions. Generate a random value with at least 32 characters.
MOCK_SESSION_SECRET=replace-with-a-random-secret-of-at-least-32-characters
